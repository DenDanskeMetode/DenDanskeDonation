name: CI - Docker Structure Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker-test:
    runs-on: ubuntu-latest

    steps:
      - name: Hent koden
        uses: actions/checkout@v4

      - name: Byg Docker images
        run: docker compose build

      - name: Installer container-structure-test
        run: |
          curl -LO https://storage.googleapis.com/container-structure-test/latest/container-structure-test-linux-amd64
          chmod +x container-structure-test-linux-amd64
          sudo mv container-structure-test-linux-amd64 /usr/local/bin/container-structure-test

      - name: Kør backend tests
        run: |
          container-structure-test test \
            --image ghcr.io/dendanskemetode/dendanskedonation/server:latest \
            --config tests/docker/backend.structure-test.yaml

      - name: Kør frontend tests
        run: |
          container-structure-test test \
            --image ghcr.io/dendanskemetode/dendanskedonation/client:latest \
            --config tests/docker/frontend.structure-test.yaml

      - name: Kør database tests
        run: |
          container-structure-test test \
            --image ghcr.io/dendanskemetode/dendanskedonation/db:latest \
            --config tests/docker/database.structure-test.yaml

      - name: Start containers
        run: |
          docker compose up -d
          sleep 5

      - name: Healthcheck backend
        run: curl -f http://localhost:5000 || exit 1

      - name: Kør Jest tests
        run: |
          cd frontend
          npm install
          npm test -- --watchAll=false